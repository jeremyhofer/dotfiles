# Repo documentation / agent context — not deployable dotfiles.
README.md
CLAUDE.md

# Compiled spell files — generated by nvim from the .add wordlists; never managed.
.config/nvim/spell/*.spl

{{ if eq .chezmoi.os "darwin" }}
.Xresources
.xinitrc
.xbindkeysrc
.config/i3/config
.config/i3status/config
.config/autostart/picom.desktop
# systemd-user ssh-agent routing — Linux-only (no systemd/environment.d on macOS,
# which uses its own launchd ssh-agent; deploying this would clobber it).
.config/environment.d/ssh-auth-sock.conf
{{ end }}

{{ if eq .domain "work" }}
# Local-dev TLS cert provisioning is personal-only (home machines); not managed on work.
setup-dev-tls.sh
{{ end }}
