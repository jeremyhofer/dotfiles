#export PATH="$HOME/.cargo/bin:$PATH"
export PATH="$PATH:$HOME/.local/bin"
export PATH="$PATH:$HOME/go/bin"

[ -f "$HOME/.dotlocal/zprofile" ] && source "$HOME/.dotlocal/zprofile"
{{- if eq .chezmoi.os "linux" }}
{{ includeTemplate "zprofile.linux" . -}}
{{- end }}
